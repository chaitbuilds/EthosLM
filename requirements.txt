# The versions the offline suites were run under.
#
# gdpc is installed on the line after this file, with --no-deps: it asks for the GUI
# build of OpenCV, which needs an X stack to import, and the headless build below
# gives the same `cv2` module without one. Both write into the same directory, so
# letting pip install gdpc's own dependency over the top of it is what breaks it.
numpy==2.5.2
scipy==1.18.0
pillow==12.3.0
matplotlib==3.11.1
scikit-image==0.26.0
opencv-python-headless==5.0.0.93
NBT==1.5.1
requests==2.34.2
PyGLM==2.8.3
Deprecated==1.3.1
more-itertools==11.1.0
typing-extensions
